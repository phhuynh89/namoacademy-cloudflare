name: Create CapCut Account

on:
  # Run every 5 minutes (GitHub Actions minimum interval)
  # Note: GitHub Actions schedules use UTC time
  # Running at :02 minutes offset to avoid conflicts with Felo workflow (which runs at :00)
  # Allow manual triggering
  workflow_dispatch:
  # Trigger on commits to trigger-action branch
  push:
    branches:
      - trigger-action

jobs:
  create-capcut-account:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Cache Puppeteer Chromium
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/puppeteer
            node_modules/.cache/puppeteer
          key: ${{ runner.os }}-puppeteer-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-puppeteer-

      - name: Install Puppeteer system dependencies
        run: |
          sudo apt-get update -qq
          # Install minimal dependencies required for headless Chrome
          # Using Puppeteer's recommended dependencies
          sudo apt-get install -y -qq \
            ca-certificates \
            fonts-liberation \
            libappindicator3-1 \
            libatk-bridge2.0-0 \
            libatk1.0-0 \
            libcairo2 \
            libcups2 \
            libdbus-1-3 \
            libexpat1 \
            libfontconfig1 \
            libgbm1 \
            libgcc-s1 \
            libglib2.0-0 \
            libgtk-3-0 \
            libnspr4 \
            libnss3 \
            libpango-1.0-0 \
            libpangocairo-1.0-0 \
            libstdc++6 \
            libx11-6 \
            libx11-xcb1 \
            libxcb1 \
            libxcomposite1 \
            libxcursor1 \
            libxdamage1 \
            libxext6 \
            libxfixes3 \
            libxi6 \
            libxrandr2 \
            libxrender1 \
            libxss1 \
            libxtst6 \
            wget \
            xdg-utils 2>&1 | grep -v "has no installation candidate" || true

      - name: Install npm dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci --prefer-offline --no-audit --no-fund
          else
            npm install --prefer-offline --no-audit --no-fund
          fi

      - name: Create .dev.vars file
        run: |
          cat > .dev.vars << EOF
          WORKER_URL=${{ secrets.WORKER_URL }}
          PUPPETEER_HEADLESS=true
          ACCOUNTS_PER_RUN=${{ secrets.ACCOUNTS_PER_RUN || '1' }}
          EOF

      - name: Run CapCut account creation
        run: npm run capcut-account-creator
        continue-on-error: true

      - name: Upload logs (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: capcut-account-creation-logs
          path: |
            logs/
          if-no-files-found: ignore
          retention-days: 7

